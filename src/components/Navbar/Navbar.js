import React from "react";
import "./Navbar.css";

const Navbar = (props) => {
    return(
        <div>
            <h1>
                Hello world
            </h1>
        </div>
    );
};

export default Navbar;
