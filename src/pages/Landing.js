import React from "react";
import NavBar from "../components/Navbar/Navbar";

const Landing = (props) => {
    return (
      <div>
       <NavBar />
      </div>
    );
  };
  
  export default Landing;